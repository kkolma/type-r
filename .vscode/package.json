{
  "name": "my-html-game",
  "version": "1.0.0",
  "description": "Egyszerű HTML/JS játék fejlesztői szerverrel",
  "scripts": {
    "start": "live-server --port=8080 --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
